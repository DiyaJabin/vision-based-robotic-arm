"""Experimental evaluation and benchmarking package.

Submodules intentionally load their optional pandas/matplotlib dependencies
only when used, so runtime-availability checks can report a clean skip.
"""
